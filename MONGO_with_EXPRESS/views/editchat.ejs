<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Your Chat</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Arial', sans-serif;
            background: linear-gradient(135deg, #fdfbfb, #ebedee);
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
            padding: 20px;
        }

        form {
            background: #ffffff;
            padding: 2rem;
            border-radius: 12px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            width: 100%;
            max-width: 400px;
        }

        h1 {
            font-size: 2rem;
            color: #333;
            margin-bottom: 1rem;
            text-align: center;
        }

        label {
            font-size: 1rem;
            color: #555;
            display: block;
            margin-bottom: 8px;
        }

        textarea {
            width: 100%;
            padding: 10px;
            border: 1px solid #ddd;
            border-radius: 8px;
            resize: none;
            font-size: 1rem;
            margin-bottom: 20px;
        }

        textarea:focus {
            border-color: #0077cc;
            outline: none;
            box-shadow: 0 0 5px rgba(0, 119, 204, 0.5);
        }

        button {
            background: #0077cc;
            color: white;
            border: none;
            padding: 12px 16px;
            font-size: 1rem;
            border-radius: 8px;
            cursor: pointer;
            width: 100%;
        }

        button:hover {
            background: #005fa3;
        }
    </style>
</head>
<body>
    <form action="http://localhost:8080/chats/<%= chat._id %>?_method=PUT" method="POST">
        <h1>Edit Your Message</h1>

        <label for="newmessage">Your New Message:</label>
        <textarea name="newmessage" id="newmessage" rows="5" placeholder="Edit your message here..."><%= chat.message %></textarea>
        
        <button type="submit">Edit</button>
    </form>
</body>
</html>
